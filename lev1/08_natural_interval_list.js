function solution(x, n) {
  let answer = [];
  for (let length = 0; length < n; length++) {
    answer.push(x * i);
  }
  return answer;
}

//다른 사람의 풀이
// function solution(x, n) {
//     return Array(n).fill(x).map((v, i) => (i + 1) * v)
// }
